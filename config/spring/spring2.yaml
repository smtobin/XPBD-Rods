name: "RodSim"

# by default, the sim attempts to run in real-time
# sim-mode: "Frame-by-frame"      # uncomment this line to run the simulation "frame by frame" (press any key on the keyboard to step forward one time step)
# sim-mode: "AFAP"                # uncomment this line to run the simulation "As Fast As Possible"

time-step: 1e-2                   # the time step used by the sim
end-time: 0.1                  # the end time in the sim (some large number)

# logging - leave disabled
logging: False
logging-output-folder: "../output/convergence"
logging-interval: 10
log-residuals: True

# simulating render settings
hdr-image-filename: "../resource/studio_1k.hdr"
create-skybox: False
hdr-scaling: 0.3

camera-focal-point: [0, 0.5, 0]
camera-position: [1, 2, 0]

ground-plane: False                # whether or not to create a ground plane

window-width: 2560
window-height: 1440
render-for-video: True
render-output-folder: "../output/render/spring"

objects:
  # === The rod object ===
  - 
    type: "Rod"
    name: "rod1"
    element-type: "Linear"        # the finite element type for the rod - "Linear", "Quadratic", "Cubic" (this is the degree of basis polynomial used)
    elements: 100                # number of finite elements used in the discretization
    initial-position: [0, 0, 0.0] # initial position of base of rod
    initial-rotation: [-5.7, 0, 0]   # initial rotation of base of rod (XYZ Euler angles, specified in degrees)
    curvature: [0,10,1]

    # material parameters
    E: 1e9                       # Young's modulus
    nu: 0.4                       # Poisson ratio
    beta: 10                     # damping coefficient in the rod
    diameter: 0.03                 # diameter of rod cross-section
    length: 10                     # rest length of rod
    density: 1000                 # density (kg/m^3)
    
    # solver parameters
    global-solve: True            # whether or not to use a global solve on the rod subsystem (should always be true)
    base-fixed: True              # whether the base should be fixed (should always be true for now)
    tip-fixed: True               # whether the tip should be fixed (should always be true for now)
    collisions: True              # whether collisions are enabled for the rod (set to true for self-collisions)
    log-particles: False          # whether to log the particles of this rod (don't need this)
    
    # render settings
    render-type: "PBR"            # use PBR rendering
    metallic: 0.0                 # metallicity of PBR material (should be 0 unless material is a metal, then it should be 1.0)
    roughness: 0.5                # roughness of rod surface in the range [0-1]
    color: [0.0, 1.0, 0]            # color of the rod
    opacity: 1                    # opacity of the rod
    draw-centerline: False        # optionally visualize the centerline of the rod
    centerline-samples: 500        # number of samples along the centerline to use for visualization
    color-elements: True
    # draw-frames: True

  # -
  #   type: "Rod"
  #   name: "Straight"
  #   element-type: "Linear"
  #   elements: 30
  #   initial-position: [0, 0, 0.0]
  #   initial-rotation: [-90, 0, 0]
  #   length: 2
  #   collisions: false